(cram
 (applies_to :whole_subtree)
 (alias construct))

; FIXME: disabled Jane Street tests that are failing internally
(cram
  (enabled_if false)
  (applies_to
     c-errors
     c-fun
     c-objects
     c-simple))
